// Stub — full implementation lands in Step 5 (background service worker
// + message bus). For now this exists only so WXT has an entrypoint to
// produce a valid manifest.
export default defineBackground(() => {
  // no-op
});
